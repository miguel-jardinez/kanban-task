export interface Spacing {
  xll: string;
  xl: string;
  lg: string;
  md: string;
  sm: string;
  xs: string;
}

export const spacing: Spacing = {
  xll: '4rem',
  xl: '3.2rem',
  lg: '2.4rem',
  md: '1.6rem',
  sm: '0.8rem',
  xs: '0.4rem',
};
